#include "screen.h"

GameScreen::GameScreen(int rows, int columns, bool debug) : _rows{rows}, _columns{columns}, _debug{debug}
{
    // _goxels = std::vector<std::vector<int>>(rows, columns);
    
}

GameScreen::~GameScreen() {}
